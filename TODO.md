- Split the big JSON into smaller portions (by categories) and save them in a separate folder for all the operations.

- Type generation from schemas, that are in the BCD repo.

- Some trickery to reduce memory usage (?)
